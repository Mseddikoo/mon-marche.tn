{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Categorie</h1>

    <table class="table table-striped table-hover">
        <tbody>

        <tr>
            <th>Nom</th>
            <td>{{ entity.nom }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('adminCategories') }}">
                Retour
            </a>
        </li>
        <li>
            <a href="{{ path('adminCategories_edit', { 'id': entity.id }) }}" class="icon-edit">

            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
